class Work {
  constructor(name, slug, cover, year, categories) {
    this.name = name;
    this.slug = slug;
    this.cover = cover;
    this.year = year;
    this.categories = categories;
  }
}

export default Work;
